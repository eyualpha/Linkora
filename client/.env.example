# Local development — Vite proxy forwards /api to your local backend
VITE_API_URL=/api

# Vercel (client project) — set in dashboard, do not commit:
# BACKEND_URL=https://your-backend.vercel.app
# Optional direct calls instead of proxy:
# VITE_API_URL=https://your-backend.vercel.app/api
